SERVER_URL=https://your-server.example.com
AGENT_LOCAL_API_PORT=3001
AGENT_LOCAL_API_HOST=127.0.0.1
DEVICE_NAME=ShotClock-01
DEFAULT_DEVICE_NAME=ShotClock-01
SETUP_AP_SSID=Shotclock-Setup
# Required in production: unique 12-63 character WPA2 password per display.
# Replaced during installation with a unique 12-character maintenance password.
SETUP_AP_PASSWORD=replace-with-unique-maintenance-password
DEVICE_AUTH_TOKEN=replace-with-64-character-random-token
NETWORK_RECOVERY_TIMEOUT_MS=120000
SETUP_PORTAL_HOST=sportsboard.local
# Optional: set this to the desktop login user if kiosk auto-detection fails.
# On the current Pi this is likely admin.
KIOSK_USER=
# MSD300-1 deployment default. This avoids motion artifacts/blue dots seen at
# higher Pi output resolutions while preserving correct RGB/BGR color handling.
KIOSK_DISPLAY_OUTPUT=auto
KIOSK_DISPLAY_MODE=1024x768
KIOSK_DISPLAY_RATE=60
KIOSK_HIDE_CURSOR=true
# Raspberry Pi 5 embedded/GPIO 5V power override. Set to 0 to skip.
PI5_PSU_MAX_CURRENT=5000
# Keep embedded Pi 5 displays booting automatically when panel power is restored.
PI5_AUTO_BOOT_ON_POWER=true
